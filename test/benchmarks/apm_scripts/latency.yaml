setup:
  steps:
  - run: |
      echo test
  - unpack_oci_artifact: ghcr.io/pawelchcki/ben-runner/example/docker_layer:latest
    dest: oci_fetch/example_path
    workdir: /tmp/target/
configurations: []
